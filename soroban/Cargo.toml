[package]
name = "chainverse-soroban-types"
version = "0.1.0"
edition = "2021"
description = "ChainVerse Academy Soroban contract types and interfaces"

[lib]
crate-type = ["lib"]

[dependencies]
soroban-sdk = "22.0.0"

[dev-dependencies]
soroban-sdk = { version = "22.0.0", features = ["testutils"] }
